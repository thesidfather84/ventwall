kind = "web"
previewPath = "/"
title = "VentWall"
version = "1.0.0"
id = "artifacts/ventwall"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22301

[services.development]
run = "pnpm --filter @workspace/ventwall run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ventwall", "run", "build" ]
publicDir = "artifacts/ventwall/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22301"
BASE_PATH = "/"
